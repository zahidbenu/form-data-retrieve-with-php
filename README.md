# form-data-retrieve-with-php
It is an Example Form and it's Data Retrieval Process with php. To retrieve form data with PHP, we can use the $_POST or $_GET superglobals based on the form's method attribute. Here I used $_POST method to retrieve the form data. In a form with the "post" method, data is sent in the HTTP request body, and we can access it using $_POST.
